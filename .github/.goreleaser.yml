release:
  github:
    owner: lmittmann
    name: tint
  draft: true
  footer: |
    **Full Changelog**: [`{{ .PreviousTag }}…{{ .Tag }}`](https://github.com/{{ .Env.GITHUB_REPOSITORY }}/compare/{{ .PreviousTag }}...{{ .Tag }})

changelog:
  sort: asc

builds:
- skip: true
